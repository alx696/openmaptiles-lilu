layer:
  id: "lilu_place"
  description: |
    里路地点
  buffer_size: 4
  fields:
    name: 名称
    type: 类型
    rank: 级别
  datasource:
    geometry_field: geometry
    query: (SELECT name, type, rank, geometry FROM layer_lilu_place(!bbox!, z(!scale_denominator!), !pixel_width!)) AS t
schema:
  - ./function.sql
datasources:
  - type: imposm3
    mapping_file: ./mapping.yaml
